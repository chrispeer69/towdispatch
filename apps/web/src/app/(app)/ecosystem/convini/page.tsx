import { EcosystemPlaceholder } from '../ecosystem-placeholder';

export const metadata = { title: 'CONVINI — Tow Dispatch' };

export default function ConviniPage(): JSX.Element {
  return (
    <EcosystemPlaceholder
      productName="CONVINI"
      accentColor="#0F9D58"
      description="CONVINI is the consumer-facing platform that coordinates towing, auto repair, body shops, rentals, insurance, travel, rewards, and event tickets under a single membership. When CONVINI integration is live, customer-requested tows from the CONVINI app will land directly in your Tow Dispatch dispatch board."
    />
  );
}
