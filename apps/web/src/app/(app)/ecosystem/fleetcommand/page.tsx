import { EcosystemPlaceholder } from '../ecosystem-placeholder';

export const metadata = { title: 'FleetCommand — US Tow DISPATCH' };

export default function FleetCommandPage(): JSX.Element {
  return (
    <EcosystemPlaceholder
      productName="FleetCommand"
      accentColor="#1E88E5"
      description="FleetCommand is the GPS, telematics, and AI dashcam platform built for towing operators. Real-time truck positions, driver behavior scoring, and dashcam evidence stream into US Tow DISPATCH so dispatch and management share a single live operating picture without switching tools."
    />
  );
}
