# Session 20 — Rebrand: TowDispatch → Tow Dispatch

Date: 2026-05-14
Branch: `feat/rename-towdispatch` (off `master`)
Posture: feature branch + PR + manual smoke test. Auto-deploy stays OFF.

---

## What shipped

Full repo-internal rebrand from **TowDispatch** → **Tow Dispatch**, executed
with a one-shot Node script (`scripts/rename-towdispatch.mjs`, deleted after
use). 387 files modified, 40 files renamed via `git mv`, no untracked
leftovers, no deleted files.

Verification:

| Check | Result |
|---|---|
| `pnpm install` (regenerates lockfile with new `@towdispatch/*` scope) | OK |
| `pnpm typecheck` (all 6 workspaces) | **clean** |
| `pnpm format` (Biome) | 219 line-ending normalizations applied |
| `pnpm lint` | 5 pre-existing style warnings (template-literal preferences, import order), none introduced by the rename |

---

## Naming conventions (per-identifier-type, applied consistently)

| Identifier type | Convention | Examples |
|---|---|---|
| Display brand (UI, docs, emails) | `Tow Dispatch` (italic on DISPATCH where styled) | layout title, login page, invoice PDF header |
| Brand all-caps (wordmarks, headers) | `Tow Dispatch` | landing hero, invoice mocks |
| Repo / npm package id | `towdispatch` (single token, no separators) | repo name, root package.json `name` |
| Workspace package scope | `@towdispatch/*` | `@towdispatch/api`, `@towdispatch/web`, `@towdispatch/shared`, `@towdispatch/db`, `@towdispatch/ui`, `@towdispatch/e2e` |
| Env var values | `towdispatch` (lowercase) | `JWT_ISSUER`, `POSTGRES_USER`, `DD_SERVICE`, etc. |
| Env var prefix | `TOWDISPATCH_` (upper, no dashes) | no keys actually use this prefix today; documented for future |
| iOS bundle ID | `com.towdispatch.driver` (+ `.tests`, `.uitests`) | `Appfile`, `project.pbxproj` build settings |
| Android applicationId / namespace | `ai.bluecollar.towdispatch.driver` | `app/build.gradle.kts` |
| Android Kotlin package path | `ai/bluecollar/towdispatch/driver/` (directory + `package` statements) | source tree moved with `git mv` |
| Domain references | `towdispatch.com` (marketing), `towdispatch.cloud` (API) | already in `app/build.gradle.kts`; verify Chris's DNS plan matches |

**App URL recommendation:** `app.towdispatch.com` (subdomain). Keeps the
marketing site (`www.towdispatch.com`) and operator console cleanly
separated, simpler CSP/CORS, easier to point a future SSO / multi-tenant
gateway at the app subdomain without re-routing the marketing root.

---

## What was renamed

### Repo-level config

- `package.json` — `name`, `description`, every `pnpm --filter @towdispatch/*` script
- `pnpm-lock.yaml` — fully regenerated by `pnpm install`
- `tsconfig.base.json` — path aliases `@towdispatch/{shared,db,ui}`
- `biome.json`, `docker-compose.yml`, `.env.example`, `.github/workflows/e2e.yml`
- `apps/*/package.json` and `packages/*/package.json` (names + workspace deps)
- `apps/*/railway.toml`, `apps/*/Dockerfile`, `apps/*/tsconfig.json`
- `README.md`, `ARCHITECTURE.md`

### Web frontend

- Browser titles, meta tags, og: tags (`apps/web/src/app/layout.tsx`)
- Landing page wordmark + header (`apps/web/src/app/page.tsx`)
- Hero wordmark component (`apps/web/src/components/brand/wordmark.tsx`)
- Login / signup / forgot / reset / forbidden / not-found / global-error
- App shell sidebar + session provider + theme assets
- Customer-facing tracking page + pay page
- Invoice header strings + email templates (`apps/api/src/modules/email/email.service.ts` → `BRAND.productName`)

### API

- All comment / log / error-message brand references
- Prometheus metric prefix: `towdispatch_api_` → `towdispatch_api_` (and matching spec)
- `JWT_ISSUER`, `JWT_AUDIENCE`, `DD_SERVICE` defaults in `config.schema.ts`
- SMTP `From:` defaults
- Every controller / service / module / guard / decorator file: comment headers, `@description`, log strings

### Mobile — Android

- Kotlin source root moved with `git mv`:
  - `apps/driver-android/app/src/main/java/ai/bluecollar/towdispatch/` → `…/ai/bluecollar/towdispatch/`
- All `package` statements + imports rewritten by the script (verified by `pnpm install` + clean dirs walk).
- Class renames + matching file renames:
  - `TowDispatchDriverApp` → `TowDispatchDriverApp` (+ file)
  - `TowDispatchApi` → `TowDispatchApi` (+ file)
  - `TowDispatchTheme` → `TowDispatchTheme` (composable)
- `Theme.TowDispatchDriver` → `Theme.TowDispatchDriver` in `themes.xml` (+ both `AndroidManifest.xml` refs)
- `AndroidManifest.xml`: `android:name=".TowDispatchDriverApp"`, `android:theme="@style/Theme.TowDispatchDriver"`
- `settings.gradle.kts`: `rootProject.name = "TowDispatchDriver"`
- `app/build.gradle.kts`: `namespace`, `applicationId`, `API_BASE_URL` (now `https://api.towdispatch.cloud`)
- `strings.xml` `app_name`, backup/data-extraction rules (db filename `towdispatch_driver.db`)

### Mobile — iOS (decisions below)

**Done in-repo (text-safe):**

- `Info.plist`: `CFBundleDisplayName`, all permission usage descriptions, NSCameraUsageDescription/NSLocationWhenInUseUsageDescription/etc.
- `Localizable.strings` (en + es): `app.name` etc.
- `project.pbxproj` build settings: `PRODUCT_BUNDLE_IDENTIFIER = com.towdispatch.driver(.tests/.uitests)`
- Fastlane `Appfile`: `app_identifier("com.towdispatch.driver")`
- Swift Package (`Packages/Core/`) protocol rename — SPM has no .pbxproj coupling, so the rename is mechanical:
  - `TowDispatchAPI` → `TowDispatchAPI`
  - `LiveTowDispatchAPI` → `LiveTowDispatchAPI`
  - File renamed: `TowDispatchAPI.swift` → `TowDispatchAPI.swift` (via `git mv`)
  - All call sites in `AuthService.swift`, `AppContainer.swift`, sync repositories, and `Tests/` updated.
- Main app `@main` struct: `TowDispatchDriverApp` → `TowDispatchDriverApp` (class + file, pbxproj path reference updated to match).

**Deferred for Xcode-driven refactor (FLAG for Chris):**

These items couple deeply to the Xcode project structure. Renaming via raw
text edits risks subtle pbxproj corruption (UUID-keyed `PBXFileReference` /
`PBXGroup` / `PBXBuildPhase` entries, scheme file names, target names,
derived-data caches). Recommended path: open in Xcode, use *Product → Scheme
→ Manage Schemes…* and the project navigator's right-click *Rename* for
files/groups, which keeps every cross-reference consistent.

| Surface | Current | Target |
|---|---|---|
| Xcode project | `TowDispatchDriver.xcodeproj/` | `TowDispatchDriver.xcodeproj/` |
| App source group / folder | `TowDispatchDriver/` | `TowDispatchDriver/` |
| Test targets | `TowDispatchDriverTests/`, `TowDispatchDriverUITests/` | `TowDispatchDriverTests/`, `…UITests/` |
| Xcode scheme | `TowDispatchDriver.xcscheme` | `TowDispatchDriver.xcscheme` |
| Path refs inside `project.pbxproj` | `TowDispatchDriver/App/...` | `TowDispatchDriver/App/...` |
| Fastlane `Fastfile` | `project: "TowDispatchDriver.xcodeproj"`, `scheme: "TowDispatchDriver"` | match new names |
| `.github/workflows/ios.yml` | `-project TowDispatchDriver.xcodeproj -scheme TowDispatchDriver` | match new names |
| `.swiftlint.yml`, `.gitignore` | included paths reference `TowDispatchDriver*` | match new names |
| `apps/driver-ios/scripts/generate-xcodeproj.py` | hard-coded folder/scheme names | match new names |

This is a single ~30-minute Xcode session for Chris. The app **runs and
builds today** with the bundle ID + display name already pointing at US Tow
DISPATCH; only the internal Xcode identifiers still spell "TowDispatch."

### Docs / runbooks / session reports

All `docs/runbooks/*`, `docs/*`, `ARCHITECTURE.md`, `README.md`, prior
`SESSION_*.md` reports, and `apps/PHASE_0_EXIT_REPORT.md` rewritten. Historical
content preserved; only brand strings changed.

### Tests / fixtures / mock data

All test descriptions, mock company strings, walkthrough docs (`apps/web/e2e/SESSION_*_WALKTHROUGH.md`), seeded data — rewritten by the bulk script.

---

## Intentionally NOT renamed (with rationale)

### 1. DB column `towdispatch_id` in `import_run_events`

`packages/db/sql/0017_import.sql:137` defines:

```sql
CREATE TABLE IF NOT EXISTS import_run_events (
  ...
  external_id text,        -- Towbook's CSV ID
  towdispatch_id uuid,      -- this row's internal DB id
  ...
);
```

`0017_import.sql` is shipped (last applied migration is `0020_mfa.sql`). Per
the rename rules: **do not rename production columns**. The matching TS field
`towdispatchId` in:

- `apps/api/src/modules/import/types.ts`
- `apps/api/src/modules/import/importers/*.importer.ts`
- `apps/api/src/modules/import/import-run.service.ts`
- `apps/api/src/modules/import/reconciliation.service.ts`
- `apps/web/src/app/(app)/import/reconcile/reconcile-client.tsx`
- `apps/web/src/app/api/import/runs/[id]/events/route.ts`

…must continue to mirror the column for the SQL queries to bind. **Cleanup is
a follow-up migration** (rename column + matching TS, single PR, ~1 hour).
Tracking note: it's only on the importer code-path, not user-visible.

### 2. iOS Xcode project structure

See "Deferred for Xcode-driven refactor" above.

### 3. `pnpm-lock.yaml` integrity hashes

The lockfile was fully regenerated by `pnpm install` after the package
renames. Any residual occurrences of "towdispatch" inside it are inside
already-resolved transitive dependency descriptors, not workspace package
names. They will not re-appear on the next install.

### 4. The `_reference/index.html` mockup

Updated by the bulk script; this is the static "vision" HTML mockup, not a
production surface. Left intact since it's historical reference material.

---

## What Chris owns (outside the repo)

1. **GitHub repo rename**: `chrispeer69/towdispatch` → `chrispeer69/towdispatch`. After the rename, update any local clones:
   ```
   git remote set-url origin https://github.com/chrispeer69/towdispatch.git
   ```
2. **Railway dashboard**: rename services (`towdispatch-api`, `towdispatch-web` if those names are used) to `towdispatch-*`. Service env vars don't reference the old name directly, but the readable label needs updating. Verify Postgres connection strings if Railway baked the old DB name in — likely `DATABASE_URL` was injected with whatever name was set when the DB was provisioned; do not change `POSTGRES_DB` on prod, only on local dev.
3. **DNS / SSL**:
   - `www.towdispatch.com` → marketing site
   - `app.towdispatch.com` → web app (recommended; see top)
   - `api.towdispatch.cloud` → API (already referenced in Android `build.gradle.kts`)
4. **Mobile bundle IDs**:
   - **iOS** `com.towdispatch.driver` is set in `Appfile` + `project.pbxproj`. There is **no prior App Store listing** under the old bundle, so this is a clean first registration. No re-submission impact.
   - **Android** `applicationId = "ai.bluecollar.towdispatch.driver"` is set in `build.gradle.kts`. Same story — no prior Play Console listing, so clean.
   - Apple App ID + Play Console listing both need to be **registered fresh** under the new bundle IDs before TestFlight / internal-track builds upload.
5. **Xcode project rename** — 30-min session per the table above.
6. **Sentry / Datadog / SendGrid sender domains** — anything keyed off the old brand name in the third-party dashboards.

---

## Open follow-ups (file in TODO.md or a fresh session)

- [ ] Xcode project structural rename (Chris, in Xcode UI)
- [ ] DB migration: rename `import_run_events.towdispatch_id` → `internal_id` (or similar) + sweep matching TS field — bundle with next non-critical-importer change
- [ ] Marketing site landing page (live `www.towdispatch.com`) — outside this repo
- [ ] Spot-check Sentry release events still tag correctly after `RELEASE_TAG` rolls forward
- [ ] One pass through `pnpm lint --apply` to mop up the 5 pre-existing style warnings (unrelated to this PR; do not bundle here)

---

## Commit / PR posture

- Branch: `feat/rename-towdispatch`
- Master is untouched.
- Auto-deploy stays OFF — this rebrand has zero customer-facing blast
  radius (zero tenants live, no public marketing surface yet) but it touches
  every workspace, so Chris should do a manual smoke pass before merging:
  1. `pnpm install && pnpm dev` → web on :3000 should render "Tow Dispatch"
     on the landing page + login page (header logo + hero wordmark + tab
     title).
  2. Hit `/login` → header reads "Tow Dispatch", subtitle is
     "Sign in to your Tow Dispatch workspace."
  3. Hit `/api/health` and `/api/ready` on :3001 → 200 ok.
  4. Open Drizzle Studio (`pnpm db:studio`) and confirm tables still load
     (sanity check that no DB-bound code accidentally renamed `towdispatch_id`).
  5. Skim `apps/driver-android/app/src/main/AndroidManifest.xml` — should
     reference `.TowDispatchDriverApp` and `@style/Theme.TowDispatchDriver`.
